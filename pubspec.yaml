name: msg_parser
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # Using a more stable SDK version range

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.0  # Moved to correct indentation level
  flutter_sms_inbox: ^1.0.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true